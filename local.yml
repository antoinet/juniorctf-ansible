- hosts: localhost
  become: true
  
  vars_files:
    - vars/main.yml

#  pre_tasks:
#    - name: update repositories
#      apt: update_cache=yes
#      changed_when: False

  roles:
    - role: geerlingguy.mysql
    - role: geerlingguy.redis
    - role: ctfd
    - role: juniorctf
